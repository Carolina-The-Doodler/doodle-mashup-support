{
  "name": "doodle-mashup-support-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Doodle Mashup support site",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^24.5.2"
  }
}
